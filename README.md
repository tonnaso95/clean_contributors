Create a new script named normalized_contributors.py .
This script must take in input @input/DIRTY_dataset_dottori_di_ricerca.xlsx, convert it into a dataframe.
A function must be written that takes as input the dataframe and operates on it.
At the end the script must save a new excel file in @output/CLEAN_dataset_dottori_di_ricerca.xlsx.
The description of the rule is in @regole_normalizzazione_ruoli_v2.md.
The input dictionary with the mapping between roles and normalized roles is in @dictionary/roles_completed.csv.
Read carefully this document and implement the rule.
